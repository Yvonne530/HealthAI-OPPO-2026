## 🖥️ 硬件架构与计算资源配额 (Hardware Specification)

本系统针对 **边缘侧异构计算单元** 进行了深度优化，以下为推荐的开发与仿真基准配置及其资源隔离策略：

### 1. 核心硬件参数

| 组件 | 规格 | 架构角色 |
| --- | --- | --- |
| **CPU** | Intel Core i7-14700HX (20C/28T) | 负责异步 IO、FNO 算子前置 FFT 计算及姿态异常逻辑检测。 |
| **GPU** | NVIDIA RTX 4070 Laptop (8GB VRAM) | 执行 MJX 物理并行仿真（256 并行数）及 Consistency Model 蒸馏训练。 |
| **RAM** | 16GB DDR5 | 采用 **静态资源隔离技术**，保障系统高负载下的稳定性。 |

### 2. 资源隔离策略 (Resource Isolation)

为了解决大规模物理仿真中 Linux 子系统与 Windows 主宿机争抢内存导致的驱动崩溃问题，系统通过 `.wslconfig` 实施了硬性配额：

* **计算沙盒 (WSL2):** 分配 **8GB 物理内存**。专门用于 MuJoCo MJX 并行计算流，确保仿真环境拥有确定的内存地址空间。
* **宿主调度 (Windows):** 保留 **8GB 物理内存**。确保显卡驱动（Kernel Mode Driver）与编译器拥有足够的交换空间，规避 OOM (Out of Memory) 风险。

### 3. 计算性能预期 (Performance Baseline)

* **MJX 仿真吞吐量:** 在 256 环境并行下，预期可实现 $>50,000$ FPS 的真值生成速率。
* **端侧推断延迟:** 配合 ncnn 的 INT8 量化，目标推断延迟控制在 **2-4ms** 范围内。


